export interface MedalData {
  name: string;
  value: number;
  extra: { id: number };
}


// Cette interface est utilisée pour définir la structure des données de médails par pays pour le graphique.
